kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mypvc
  namespace: nginx
spec: 
  accessModes
    - ReadWriteOnce
  resources:
    request:
     storage: 1Gi
  storageClassName: local-storage